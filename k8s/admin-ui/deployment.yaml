apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-ui
  namespace: ecommerce
  labels:
    app: shopnow
    component: admin-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shopnow
      component: admin-ui
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: shopnow
        component: admin-ui
    spec:
      serviceAccountName: shopnow-admin-ui
      securityContext:
        runAsNonRoot: true
      initContainers:
        - name: wait-for-api
          image: busybox:1.36
          securityContext:
            runAsUser: 65534
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          command:
            - sh
            - -c
            - |
              until nc -z api 8000; do
                echo "API not ready — retrying in 3s"
                sleep 3
              done
              echo "API is ready."
          resources:
            requests:
              cpu: "10m"
              memory: "16Mi"
            limits:
              cpu: "50m"
              memory: "32Mi"
      containers:
        - name: admin-ui
          image: ghcr.io/johnitopaisah/ecommerce-admin-ui:v0.1.4-rc2
          imagePullPolicy: Always
          securityContext:
            runAsUser: 1001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 3001
              name: http
          env:
            - name: PORT
              value: "3001"
            - name: NODE_ENV
              value: "production"
            - name: INTERNAL_API_URL
              value: "http://api:8000"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          # livenessProbe:
          #   httpGet:
          #     # basePath is /admin-panel so probe must use that path
          #     path: /admin-panel
          #     port: 3001
          #   initialDelaySeconds: 30
          #   periodSeconds: 15
          #   timeoutSeconds: 5
          #   failureThreshold: 3
          # readinessProbe:
          #   httpGet:
          #     path: /admin-panel
          #     port: 3001
          #   initialDelaySeconds: 15
          #   periodSeconds: 10
          #   timeoutSeconds: 5
          #   failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
