apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shopnow-redis
  namespace: argocd
  labels:
    app.kubernetes.io/name: shopnow-redis
    app.kubernetes.io/part-of: shopnow
    app.kubernetes.io/component: redis
    app.kubernetes.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"   # cache tier ready before app services
  # No finalizer — ArgoCD must never auto-delete Redis data
spec:
  project: shopnow
  source:
    repoURL: https://github.com/johnitopaisah/ecommerce-platform
    targetRevision: develop
    path: k8s/redis
  destination:
    server: https://kubernetes.default.svc
    namespace: ecommerce
  # NO automated sync — Redis changes require a manual review + click in ArgoCD UI
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      name: redis
      jsonPointers:
        - /spec/replicas
        - /spec/volumeClaimTemplates
