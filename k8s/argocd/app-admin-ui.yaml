apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shopnow-admin-ui
  namespace: argocd
  labels:
    app.kubernetes.io/name: shopnow-admin-ui
    app.kubernetes.io/part-of: shopnow
    app.kubernetes.io/component: admin-ui
    app.kubernetes.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: shopnow
  source:
    repoURL: https://github.com/johnitopaisah/ecommerce-platform
    targetRevision: develop
    path: k8s/admin-ui
  destination:
    server: https://kubernetes.default.svc
    namespace: ecommerce
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=false
  ignoreDifferences:
    - group: apps
      kind: Deployment
      name: admin-ui
      jsonPointers:
        - /spec/replicas   # HPA owns this at runtime
